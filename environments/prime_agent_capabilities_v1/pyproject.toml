[project]
name = "prime-agent-capabilities-v1"
version = "0.1.0"
description = "Executable Prime Agent harness capability tasks"
requires-python = ">=3.11"
dependencies = ["verifiers"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["prime_agent_capabilities_v1"]
