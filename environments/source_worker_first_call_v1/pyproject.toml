[project]
name = "source-worker-first-call-v1"
version = "0.1.0"
description = "Execution-grounded first-call source-worker GRPO reward."
requires-python = ">=3.11"
dependencies = ["verifiers", "subagent-communication-v1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["source_worker_first_call_v1"]
