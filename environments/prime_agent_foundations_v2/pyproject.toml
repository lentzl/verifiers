[project]
name = "prime-agent-foundations-v2"
version = "0.1.0"
description = "Observable foundation tasks for the official Prime Agent harness."
requires-python = ">=3.11"
dependencies = ["verifiers"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["prime_agent_foundations_v2"]
