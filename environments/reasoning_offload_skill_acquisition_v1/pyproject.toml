[project]
name = "reasoning-offload-skill-acquisition-v1"
version = "0.1.0"
description = "Hidden-family tasks for evaluating portable persistent skill acquisition."
requires-python = ">=3.11"
dependencies = ["verifiers"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["reasoning_offload_skill_acquisition_v1"]
