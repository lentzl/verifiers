[project]
name = "document-translation-v1"
version = "0.1.0"
description = "Prime Agent document-owner and delegated translation development task."
requires-python = ">=3.11"
dependencies = ["verifiers"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["document_translation_v1"]
