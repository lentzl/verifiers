[project]
name = "subagent-communication-v1"
version = "0.1.0"
description = "Native Prime Agent RLM delegation and family-message curriculum."
requires-python = ">=3.11"
dependencies = ["verifiers"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["subagent_communication_v1"]
