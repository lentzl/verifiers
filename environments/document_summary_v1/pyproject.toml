[project]
name = "document-summary-v1"
version = "0.1.0"
description = "Prime Agent chapter summarization and aggregation task."
requires-python = ">=3.11"
dependencies = ["verifiers"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["document_summary_v1"]
